import { ProfileFavoritesView } from '../features/profile-favorites'

/** Route: `/favorites` — composition only (FSD: page → feature view). */
export const FavoritesPage = () => <ProfileFavoritesView />
